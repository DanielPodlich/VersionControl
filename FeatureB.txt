class FeatureB {

    void execute(Object input) {

        if (validate(input)) {
            save(input);
        }
    }

    void save(Object output) {
        // save result
    }

    boolean validate(Object input) {
        // nicht kompilierfähige Validierungslogik
        return true;
    }

}
